version: "3.5"
services:
  filmmaker_api:
    build: .
    ports:
      - "5002:5001"
    networks:
      - dudutech_network
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure


networks:
  dudutech_network:
    name: dudutech_network